window.addEventListener('pywebviewready', () => {
    //! get user information
    //! than render it
    // following function expects a user name and password hash
    // than returns the information in the form of JSON
    // pywebview.api.get_user_info()
})